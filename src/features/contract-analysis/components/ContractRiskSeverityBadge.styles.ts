import { SxProps, Theme } from '@mui/material';

export const contractRiskSeverityBadgeStyles: {
  severityChip: SxProps<Theme>;
} = {
  severityChip: {
    fontWeight: 600,
    fontSize: '0.7rem',
    letterSpacing: '0.05em',
    textTransform: 'uppercase',
  },
};
